{
  "name": "jobhazel-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm run typecheck && node scripts/build.mjs production",
    "build:dev": "npm run typecheck && node scripts/build.mjs development",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "typescript": "^5.9.3"
  }
}
